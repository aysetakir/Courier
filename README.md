# Courier

Async/await HTTP client for Swift.

> Yapım aşamasında. Öğrenme amaçlı yazılıyor.

## Yol haritası

- [ ] v0.1 — `Endpoint`, `URLRequest` üretimi, `HTTPClient`, hata taksonomisi, `URLProtocol` ile testler
- [ ] v1.0 — Interceptor zinciri, auth header, single-flight token refresh, retry + backoff
- [ ] v1.1 — Multipart upload, request/response logger
